# MPT Simulator (現代ポートフォリオ理論シミュレータ) 開発計画

このドキュメントは、現代ポートフォリオ理論 (Modern Portfolio Theory: MPT) をベースにしたiOSアプリをFlutterで構築するための設計および開発計画です。
AIを活用した分析機能（Gemini API や Apple Intelligence を見据えた設計）も含めて設計しています。

---

## 1. プロジェクト概要

### 目的
投資家や学習者が、複数の資産（アセット）を組み合わせた際の「リスク・リターン」を視覚的にシミュレーションし、現代ポートフォリオ理論に基づく「最適ポートフォリオ（効率的フロンティア）」を算出・分析できるiOS/Androidアプリを開発します。

### 主要機能
1. **アセット定義**: 複数の資産クラス（米国株、日本株、金、債券など）を定義し、それぞれの「期待リターン」「リスク（標準偏差）」「相関係数」を設定・管理する。
2. **ポートフォリオシミュレーション**: 
   - ユーザーがアロケーション（配分比率）を手動調整した際のリスク・リターンを即座に計算。
   - モンテカルロ・シミュレーションを用いて、数千通りのランダム配分から「効率的フロンティア（Efficient Frontier）」を生成。
3. **最適ポートフォリオの算出**:
   - シャープ・レシオ (Sharpe Ratio) が最大となる「接点ポートフォリオ (Tangency Portfolio)」の自動算出。
   - 最小分散ポートフォリオ (Minimum Variance Portfolio) の自動算出。
4. **AI分析・アドバイザー (Apple Intelligence / Gemini 連携)**:
   - ポートフォリオの構成、リスク・リターン特性をLLMにインプットし、リバランス提案や市況に応じたアロケーションのアドバイスをテキストで生成。
   - iOSの **App Intents** を見据えたデータ構造 of iOSネイティブ連携。

---

## 2. 技術スタック

| レイヤー | 技術 / ライブラリ | 備考 |
| :--- | :--- | :--- |
| **クロスプラットフォーム** | Flutter (Dart) | iOSファーストで開発しつつAndroidも視野に |
| **状態管理** | `flutter_riverpod` | 状態の分離とテスト容易性を確保 |
| **グラフ・可視化** | `fl_chart` | 効率的フロンティアの散布図、アロケーションの円グラフ |
| **数値計算** | 自作Dartロジック | 行列演算、モンテカルロ法によるポートフォリオ計算 |
| **AI機能** | `google_generative_ai` (Gemini API) | ローカルでの素早いプロトタイピングと高度な分析 |
| **iOS連携 (AI)** | Swift MethodChannel / App Intents | iOSネイティブのApple Intelligence / Siriとの将来的な統合準備 |

---

## 3. アプリの構成と画面遷移

- **ダッシュボード / 保有ポートフォリオ**: 現在のポートフォリオ配分（円グラフ）、期待リターン、リスク、シャープ・レシオの表示。AIによるワンタップクイック診断。
- **アセットマネージャー**: 資産（例：S&P500, TOPIX, 金, 米国債など）の追加・編集。各資産のリターン・リスク、相関係数マトリクス（-1.0〜1.0）の設定。
- **効率的フロンティア・シミュレータ**: `fl_chart` を使用した2次元散布図。モンテカルロシミュレーションで生成した数千個のポートフォリオ、接点ポートフォリオ、最小分散ポートフォリオ、現在のポートフォリオをプロット。
- **AI ポートフォリオ診断**: チャットUIまたはレポート生成画面。ポートフォリオ情報からAIがアドバイスを生成。

---

## 4. 現代ポートフォリオ理論 (MPT) 計算仕様

ポートフォリオの期待リターン $E(R_p)$ と分散 $\sigma_p^2$ は以下の数式に基づき計算します。

1. **ポートフォリオの期待リターン**:
   $$E(R_p) = \sum_{i=1}^{n} w_i E(R_i)$$
   ($w_i$: 資産 $i$ のウェイト, $E(R_i)$: 資産 $i$ の期待リターン)

2. **ポートフォリオの分散 (リスクの二乗)**:
   $$\sigma_p^2 = \sum_{i=1}^{n} \sum_{j=1}^{n} w_i w_j \sigma_i \sigma_j \rho_{ij}$$
   ($\sigma_i$: 資産 $i$ の標準偏差, $\rho_{ij}$: 資産 $i$ と $j$ の相関係数)

3. **シャープ・レシオ (Sharpe Ratio)**:
   $$SR_p = \frac{E(R_p) - R_f}{\sigma_p}$$
   ($R_f$: 無リスク資産利回り。デフォルトは 0% または 1% など)

---

## 5. 開発ロードマップ

- **Phase 1: 基礎ロジック & UI構築**: Flutterプロジェクトの初期設定、Riverpodの導入。資産データモデル (`Asset`, `Portfolio`) の作成。MPT計算ロジックの実装とテスト。
- **Phase 2: モンテカルロ & 効率的フロンティア描画**: モンテカルロシミュレーションエンジンの実装（数千点のランダムウェイト計算）。`fl_chart` を用いた効率的フロンティア散布図の実装。
- **Phase 3: AI分析 (Gemini API) の統合**: Gemini APIクライアントの実装。AI分析用システムプロンプトの設計。ポートフォリオ診断レポート表示画面の実装。
- **Phase 4: iOS ネティブ連携・Apple Intelligenceの準備**: iOS App Intents の Swift 実装とFlutterのデータ連携。UI/UXの美装化。
